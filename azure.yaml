# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: context7-mcp-http-typescript
metadata:
  template: azd-init@1.15.1
services:
  context7-mcp-http-typescript:
    project: .
    host: containerapp
    language: js
    docker:
      path: Dockerfile
